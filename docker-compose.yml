version: '3.8'
services:
  bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    ports:
      - "8082:8080"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
